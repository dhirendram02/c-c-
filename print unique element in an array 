#include<stdio.h>
int main()
{
    int arr[30];
    int i,j,n,c;
    scanf("%d",&n);
    
    for(i=0;i<n;i++)
    {
       scanf("%d",&arr[i]); 
    }
    for(i=0;i<n;i++)
    {
        c=0;
        for(j=0;i<i;j++)
        {
          if(arr[i]==arr[j])
          {
              c=1;
              break;
          }
        }
          if (c==0)
          
           printf("%d\t",arr[i]); 
          
        
    }
    
return 0;
}